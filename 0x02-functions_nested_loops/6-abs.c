#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - function computs the absulute value of an integer.
 * @r:^parameter used.
 *
 * Return: 0.
 */
int _abs(int r)
{
	_abs(r);
	_putchar(r);
	_putchar('\n');
	return (0);
}
