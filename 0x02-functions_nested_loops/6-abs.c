#include "main.h"
#include <limits.h>
#include <stdio.h>

/**
 * _abs - function computs the absulute value of an integer.
 * @r: parameter used.
 *
 * Return: 0.
 */
int _abs(int r)
{
	int n = _abs(r);

	_putchar(n);
	_putchar('\n');
	return (0);
}
