#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - function computs the absulute value of an integer.
 * @n: parameter used.
 *
 * Return: 0.
 */
int _abs(int n)
{
	int r = abs(n);

	_putchar(r);
	_putchar('\n');
	return (0);
}
