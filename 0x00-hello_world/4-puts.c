#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char hello[] = "Programming is like building multilingual puzzle";	
	puts(hello);
    	return(0);
}
